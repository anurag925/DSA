package dsa

import (
	"fmt"
)

func MakeGood(s string) string {
	fmt.Println(rune('A'), rune('Z'), rune('a'), rune('z'))
	// st := make([]rune, 0)
	// for _, e:=range s{
	//   if len(st) > 0{
	//     top := st[len(st)-1]
	// 		if top >= 65 && top <
	//   }
	//   st = append(st, e)
	// }
	return ""
}
